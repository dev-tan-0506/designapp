{
  "name": "@design-editor/web",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "next build",
    "dev": "next dev --turbo",
    "lint": "eslint . --ext .ts,.tsx --max-warnings 0",
    "test": "vitest run --passWithNoTests",
    "docker-build": "docker build -f Dockerfile ../.."
  },
  "dependencies": {
    "@design-editor/common-types": "workspace:*",
    "@design-editor/ui": "workspace:*",
    "next": "^15.1.6",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  }
}

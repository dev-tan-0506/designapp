{
  "name": "@design-editor/canvas-engine",
  "version": "0.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --format esm,cjs --dts --out-dir dist",
    "lint": "eslint src --ext .ts --max-warnings 0",
    "test": "vitest run"
  },
  "dependencies": {
    "@design-editor/common-types": "workspace:*"
  },
  "peerDependencies": {}
}
